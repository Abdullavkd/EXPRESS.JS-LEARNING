{
  "name": "express.js-learning",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node server",
    "dev": "node --watch server",
    "test": "node --watch --env-file=.env serverTwo",
    "one" : "node --watch --env-file=.env serverThree",
    "two" : "node --watch --env-file=.env data",
    "sender" : "node --watch --env-file=.env newRouterServer",
    "middle" : "node --watch --env-file=.env middlewareTest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}
